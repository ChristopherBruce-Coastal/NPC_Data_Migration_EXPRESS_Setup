<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>HarborMig_Email_Format</fullName>
    <active>true</active>
    <description>Tier-2 (F7): rejects malformed emails on upsert. Seed-safe.</description>
    <errorConditionFormula>AND(NOT(ISBLANK(Email)), OR(NOT(CONTAINS(Email, "@")), NOT(CONTAINS(RIGHT(Email, LEN(Email) - FIND("@", Email)), ".")), CONTAINS(Email, " "), CONTAINS(Email, ".."), CONTAINS(Email, "@@")))</errorConditionFormula>
    <errorDisplayField>Email</errorDisplayField>
    <errorMessage>Email address is not in a valid format. Cleanse the source email before migrating (check for a missing @, missing domain dot, spaces, or double dots).</errorMessage>
</ValidationRule>
