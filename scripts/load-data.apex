// Seed the Coastie-Ed TARGET dataset (one-shot per org).
// Run in Developer Console > Execute Anonymous, or:
//   sf apex run --file scripts/load-data.apex -o <alias>
CoastieEdTrainingDataLoader.run();
